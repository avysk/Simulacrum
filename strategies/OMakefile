OCAMLINCLUDES += ../definitions/

FILES[] = $(removesuffix $(glob *.ml))

LIBRARY = libstrategy

OCamlLibrary($(LIBRARY), $(FILES))

clean:
	rm -f $(filter-proper-targets $(glob $(addsuffix .*, $(FILES)))) $(LIBRARY).*
